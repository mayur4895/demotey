// theme.ts
import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#1976d2", // static color values
    },
    secondary: {
      main: "#dc004e", // static color values
    },
    
  },
  typography: {
     
  },
 
});
